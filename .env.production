# Production Environment Variables (used by `npm run build`)
# ORIGIN ONLY — do NOT include /api/v1 here; src/config/index.js appends it.
VITE_API_BASE_URL=https://backend-epasal.onrender.com

VITE_APP_NAME=Epasaley
VITE_APP_DESCRIPTION=Nepal's Trusted Online Store
